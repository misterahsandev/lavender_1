import 'package:flutter/material.dart';
import 'package:resize/resize.dart';

class Custom_orderState {
  TextStyle ? headingTitle;
  Custom_orderState() {
    ///Initialize variables
    headingTitle = TextStyle( color: Colors.grey,fontSize: 13.sp,fontWeight:FontWeight.w400);
  }
}
